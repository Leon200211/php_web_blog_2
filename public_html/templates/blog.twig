{% extends 'base.twig' %}

{% block body %}
    <div class="container">
        <h1 class="mb-3 mt-3">Блог</h1>
        <div class="row">
            <div class="col-md-9">
                {% include 'section/posts.twig' with posts %}
                <div class="row">
                    {% include 'section/pagination.twig' with pagination %}
                </div>
            </div>
            <div class="col-md-3">
                {% include 'section/about.twig' %}
            </div>
        </div>
    </div>
{% endblock %}